/// Enum representing different sources for images.
enum ImageSources {
  /// Source for images from network.
  network,

  /// Source for images from assets.
  asset,

  /// Source for system images.
  system,
}
